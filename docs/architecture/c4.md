# Arquitectura C4

Este documento describe la aplicacion en 4 niveles C4: contexto, contenedores, componentes y codigo.

## Nivel 1: Contexto

```mermaid
C4Context
title App Trafico MKT - Contexto

Person(solicitante, "Solicitante", "Registra requerimientos de actividades o eventos.")
Person(tecnico, "Equipo tecnico", "Gestiona productos, evidencias y avances.")
Person(aprobador, "Aprobador", "Aprueba o rechaza productos con evidencias.")
Person(coordinador, "Coordinador", "Supervisa todos los requerimientos y productos.")
Person(admin, "Administrador", "Administra usuarios, roles, catalogos, marca y tracking.")

System(app, "App Trafico MKT", "Plataforma para gestionar requerimientos, productos, evidencias, aprobaciones, auditoria y metricas.")

System_Ext(microsoft, "Microsoft Entra ID / Office 365", "Inicio de sesion SSO organizacional.")
System_Ext(powerAutomate, "Power Automate", "Envio de notificaciones por correo y Teams.")
System_Ext(storage, "Almacenamiento externo", "Local, Blob Storage o FTP para evidencias.")
System_Ext(powerBi, "Power BI", "Analitica y visualizacion ejecutiva.")

Rel(solicitante, app, "Crea y consulta requerimientos")
Rel(tecnico, app, "Gestiona productos y adjunta evidencias")
Rel(aprobador, app, "Revisa evidencias y aprueba o rechaza")
Rel(coordinador, app, "Supervisa operacion completa")
Rel(admin, app, "Configura y audita")
Rel(app, microsoft, "Autentica usuarios", "OIDC")
Rel(app, powerAutomate, "Dispara notificaciones", "Webhook HTTP")
Rel(app, storage, "Guarda y consulta adjuntos")
Rel(powerBi, app, "Consume datos de metricas/auditoria")
```

## Nivel 2: Contenedores

```mermaid
C4Container
title App Trafico MKT - Contenedores

Person(user, "Usuarios UTI", "Solicitantes, tecnicos, aprobadores, coordinadores y administradores.")

System_Boundary(platform, "App Trafico MKT") {
  Container(web, "Frontend Web", "Next.js", "Pantallas de gestion, administracion, metricas y auditorias.")
  Container(gateway, "Nginx", "Reverse proxy", "Entrada HTTP/HTTPS para frontend y APIs.")
  Container(identity, "Identity API", ".NET", "Login, JWT, usuarios, roles, permisos, marca y SSO.")
  Container(reqApi, "Requirements API", ".NET", "Gestion de requerimientos y auditoria de requerimientos.")
  Container(actApi, "Activities API", ".NET", "Gestion de productos, workflow y notificaciones.")
  Container(evidenceApi, "Evidence API", ".NET", "Carga de adjuntos, aprobaciones y auditoria de aprobaciones.")
  Container(adminApi, "Administration API", ".NET", "Catalogos, facultades, sedes, carreras, aprobadores y carga inicial.")
  ContainerDb(sql, "SQL Server", "SQL Server", "Bases logicas: RequirementsDb, ActivitiesDb, EvidenceDb, IdentityDb, AdministrationDb.")
}

System_Ext(microsoft, "Microsoft Entra ID", "SSO Office 365")
System_Ext(powerAutomate, "Power Automate", "Correo y Teams")
System_Ext(blobFtp, "Blob Storage / FTP / Local", "Repositorio de archivos")

Rel(user, web, "Usa la aplicacion", "HTTPS")
Rel(web, gateway, "Consume APIs", "HTTP/HTTPS")
Rel(gateway, identity, "Proxy /api/identity")
Rel(gateway, reqApi, "Proxy /api/requirements")
Rel(gateway, actApi, "Proxy /api/activities")
Rel(gateway, evidenceApi, "Proxy /api/evidence y /api/approvals")
Rel(gateway, adminApi, "Proxy /api/admin")
Rel(identity, sql, "Lee/escribe usuarios, roles, marca")
Rel(reqApi, sql, "Lee/escribe requerimientos")
Rel(actApi, sql, "Lee/escribe productos")
Rel(evidenceApi, sql, "Lee/escribe adjuntos y aprobaciones")
Rel(adminApi, sql, "Lee/escribe catalogos y carga inicial")
Rel(identity, microsoft, "Valida SSO", "OIDC")
Rel(actApi, powerAutomate, "Envia aprobaciones", "Webhook")
Rel(evidenceApi, blobFtp, "Carga/consulta archivos")
```

## Nivel 3: Componentes

```mermaid
C4Component
title App Trafico MKT - Componentes principales

Container_Boundary(frontend, "Frontend Next.js") {
  Component(nav, "AppNav", "React", "Menu por permisos, marca institucional e idioma.")
  Component(reqPage, "Requerimientos", "React Page", "CRUD, tracking y cambios de estado.")
  Component(prodPage, "Productos", "React Page", "CRUD, adjuntos, aprobaciones y versiones enviadas.")
  Component(evidencePage, "Adjuntos", "React Page", "Cabecera-detalle de evidencias, preview y eliminacion logica.")
  Component(approvalPage, "Aprobaciones", "React Page", "Revision de producto, adjuntos y decision.")
  Component(auditPage, "Auditorias", "React Page", "Tracking consolidado de requerimientos, productos y aprobaciones.")
  Component(adminPages, "Administracion", "React Pages", "Catalogos, usuarios, marca, almacenamiento, notificaciones y carga inicial.")
}

Container_Boundary(backend, "Microservicios .NET") {
  Component(identityComp, "Identity endpoints", "Minimal API", "Auth local, SSO, usuarios, roles y marca.")
  Component(reqComp, "Requirement workflow", "Minimal API + Domain", "Estados, auditoria y metricas de requerimientos.")
  Component(actComp, "Activity workflow", "Minimal API + Domain", "Estados de productos, aprobaciones, notificaciones y sync con requerimiento.")
  Component(evidenceComp, "Evidence workflow", "Minimal API", "Archivos, storage local/cloud, aprobaciones y auditoria.")
  Component(adminComp, "Catalog/import workflow", "Minimal API", "Catalogos, relaciones FK y carga inicial Excel.")
  Component(domain, "BuildingBlocks", "C# Domain", "Entidades, enums, reglas de workflow y catalogos base.")
}

Rel(nav, identityComp, "Consulta marca/permisos")
Rel(reqPage, reqComp, "Gestiona requerimientos")
Rel(prodPage, actComp, "Gestiona productos")
Rel(prodPage, evidenceComp, "Carga y consulta adjuntos")
Rel(approvalPage, actComp, "Aprueba/rechaza productos")
Rel(approvalPage, evidenceComp, "Consulta adjuntos/aprobaciones")
Rel(auditPage, reqComp, "Lee auditoria requerimientos")
Rel(auditPage, actComp, "Lee auditoria productos")
Rel(auditPage, evidenceComp, "Lee auditoria aprobaciones")
Rel(adminPages, adminComp, "Gestiona catalogos y carga inicial")
Rel(identityComp, domain, "Usa roles/permisos")
Rel(reqComp, domain, "Usa reglas de requerimiento")
Rel(actComp, domain, "Usa reglas de producto")
```

## Nivel 4: Codigo

```mermaid
classDiagram
direction LR

class Requirement {
  +Guid Id
  +string Code
  +string ActivityOrEvent
  +RequirementStatus Status
  +StartAnalysis()
  +StartExecution()
  +Complete(total, approved)
  +Delete(user)
}

class TechnicalActivity {
  +Guid Id
  +Guid RequirementId
  +string ProductId
  +ActivityStatus Status
  +Start()
  +MarkEvidenceAttached()
  +SendToApproval()
  +Decide(decision)
  +Delete(user)
}

class EvidenceItem {
  +Guid Id
  +Guid ActivityId
  +string FileName
  +string StorageUrl
  +Delete(user)
}

class ActivityApproval {
  +Guid Id
  +Guid ActivityId
  +ApprovalDecision Decision
  +string ApprovedBy
  +string Comments
}

class AppUser {
  +Guid Id
  +string Email
  +string Roles
  +string ScreenPermissions
  +string MenuMode
  +bool IsActive
}

class CatalogReference {
  +Guid Id
  +string Type
  +string Code
  +string Name
}

class AuditEvent {
  +Guid Id
  +Guid EntityId
  +string FromStatus
  +string ToStatus
  +string Action
  +string PerformedBy
  +DateTimeOffset OccurredAt
}

Requirement "1" --> "0..*" TechnicalActivity : agrupa
TechnicalActivity "1" --> "0..*" EvidenceItem : adjuntos
TechnicalActivity "1" --> "0..*" ActivityApproval : versiones/aprobaciones
Requirement "1" --> "0..*" AuditEvent : tracking
TechnicalActivity "1" --> "0..*" AuditEvent : tracking
TechnicalActivity "*" --> "*" CatalogReference : catalogos FK
AppUser "*" --> "*" TechnicalActivity : responsable
```

## Vista de despliegue

```mermaid
flowchart TB
  subgraph Clientes
    Browser[Navegador web]
    Teams[Microsoft Teams iframe]
    PowerBI[Power BI Desktop/Service]
  end

  subgraph RedInterna[Equipo MarketingIndo / Docker Desktop]
    Nginx[Nginx :80/:443]
    Web[Next.js :3000]
    Req[Requirements API :5101]
    Act[Activities API :5102]
    Ev[Evidence API :5103]
    Id[Identity API :5104]
    Adm[Administration API :5105]
    SQL[(SQL Server :14333)]
    Files[(Volumen evidencias)]
    Tunnel[cloudflared]
  end

  Browser -->|HTTPS MarketingIndo| Nginx
  Teams -->|HTTPS + CSP frame-ancestors| Nginx
  Tunnel -->|HTTPS interno| Nginx
  Nginx --> Web
  Nginx --> Req
  Nginx --> Act
  Nginx --> Ev
  Nginx --> Id
  Nginx --> Adm
  Req --> SQL
  Act --> SQL
  Ev --> SQL
  Id --> SQL
  Adm --> SQL
  Ev --> Files
  PowerBI --> SQL
```

Entradas soportadas:

- `https://MarketingIndo`: nombre principal de red interna.
- `https://DESKTOP-Q1VCG41`: nombre secundario.
- `https://localhost`: administración desde el servidor.
- `https://marketingtrafico.indoamerica.edu.ec`: hostname previsto para túnel nombrado.

## Secuencia de aprobación

```mermaid
sequenceDiagram
  actor T as Técnico
  participant W as Next.js
  participant A as Activities API
  participant E as Evidence API
  participant N as Power Automate
  actor P as Aprobador

  T->>W: Adjunta archivo o URL
  W->>E: POST /evidence o /evidence/upload
  E-->>W: Evidencia registrada
  W->>A: PATCH evidence-attached
  T->>W: Enviar a aprobación
  W->>A: PATCH submit-approval
  A->>N: Webhook HTML/Teams
  N-->>P: Notificación
  P->>W: Revisa producto y adjuntos
  W->>A: POST /activities/{id}/approvals
  A->>E: Registra decisión/versionado
  A-->>W: Estado actualizado
```

## Límites de responsabilidad

| Límite | Propietario | Datos principales |
| --- | --- | --- |
| Identidad | Identity API | Usuarios, roles, permisos, marca, sesión y SSO. |
| Requerimientos | Requirements API | Solicitud, estados y auditoría del requerimiento. |
| Productos | Activities API | Asignación, workflow, notificaciones y auditoría del producto. |
| Evidencias | Evidence API | Adjuntos, storage, decisiones y auditoría de aprobación. |
| Parametrización | Administration API | Catálogos, relaciones y cargas iniciales. |

## Decisiones arquitectónicas

1. Cada microservicio conserva su base lógica para reducir acoplamiento de escritura.
2. Nginx actúa como único punto de entrada y resuelve dinámicamente los servicios Docker.
3. Las eliminaciones son lógicas para preservar integridad y trazabilidad.
4. Los catálogos principales se relacionan mediante FK y no por texto libre.
5. Los archivos se abstraen mediante proveedor Local, Blob o FTP.
6. Los eventos de auditoría conservan estado, usuario, fecha y JSON de contexto.
7. `restart: unless-stopped` mantiene los contenedores disponibles mientras Docker Desktop esté iniciado.
8. La política CSP permite integrar la aplicación como iframe de Microsoft Teams y Microsoft 365.

