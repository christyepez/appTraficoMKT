import{render,screen}from"@testing-library/react";import userEvent from"@testing-library/user-event";import{describe,expect,it,vi}from"vitest";import{NotificationLog}from"./NotificationLog";const workspace={items:[],loading:true,refreshing:false,error:"",reload:vi.fn()};describe("NotificationLog",()=>{it("muestra carga, error y reintento",async()=>{const user=userEvent.setup(),view=render(<NotificationLog workspace={workspace}/>);expect(screen.getByRole("status")).toBeInTheDocument();view.rerender(<NotificationLog workspace={{...workspace,loading:false,error:"Error API"}}/>);await user.click(screen.getByRole("button",{name:"Reintentar"}));expect(workspace.reload).toHaveBeenCalled();});it("muestra vacío y filtro",async()=>{const user=userEvent.setup();render(<NotificationLog workspace={{...workspace,loading:false}}/>);expect(screen.getByText("Sin registros.")).toBeInTheDocument();await user.type(screen.getByLabelText("Buscar"),"evento");expect(screen.getByLabelText("Buscar")).toHaveValue("evento");});});
