import type { ReactNode } from "react"; import { AccessibleDialog } from "../../../shared/components/AccessibleDialog";
export function AdministrationDialog({ title, saving, onClose, children }: { title: string; saving: boolean; onClose: () => void; children: ReactNode }) { return <AccessibleDialog labelledBy="administration-form-title" onClose={onClose} closeDisabled={saving}><div className="card-head"><h2 id="administration-form-title">{title}</h2><button autoFocus className="icon-button" type="button" aria-label="Cerrar formulario" disabled={saving} onClick={onClose}>×</button></div>{children}</AccessibleDialog>; }
