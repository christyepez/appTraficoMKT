import type { CatalogGroup, CatalogKind } from "../models/administration.models";
export function CatalogGroupSelector({ groups, kind, type, onSelect }: { groups: CatalogGroup[]; kind: CatalogKind; type: string; onSelect: (group: CatalogGroup) => void }) { return <nav className="catalog-tabs catalog-tabs-horizontal top-space" aria-label="Grupos de catálogos">{groups.map((group) => { const active = kind === group.kind && (group.kind !== "catalogs" || group.type === type); return <button className={active ? "tab active" : "tab"} aria-current={active ? "page" : undefined} key={`${group.kind}-${group.type ?? group.label}`} type="button" onClick={() => onSelect(group)}>{group.label}</button>; })}</nav>; }
