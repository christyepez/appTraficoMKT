import { describe, expect, it } from "vitest"; import { administrationFormSchema } from "./administration.schema";
describe("administration schema", () => { it("valida cada variante", () => { expect(administrationFormSchema.safeParse({ variant: "faculties", code: "ING", name: "Ingeniería", isActive: true }).success).toBe(true); expect(administrationFormSchema.safeParse({ variant: "careers", code: "SOFT", name: "Software", facultyId: "", isActive: true }).success).toBe(false); expect(administrationFormSchema.safeParse({ variant: "catalogs", type: "", code: "A", name: "A", isActive: true }).success).toBe(false); expect(administrationFormSchema.safeParse({ variant: "approvers", name: "Ana", email: "bad", approvalLevel: 0, facultyId: "", isActive: true }).success).toBe(false); }); });
