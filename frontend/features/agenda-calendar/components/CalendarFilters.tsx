import { Search } from "lucide-react";
import type { Technician } from "../../agenda/models/agenda.models";
import { calendarStatusLabel } from "../utils/calendar.utils";
export function CalendarFilters({ technicians, campuses, statuses, technician, campus, status, search, onTechnician, onCampus, onStatus, onSearch }: { technicians: Technician[]; campuses: string[]; statuses: string[]; technician: string; campus: string; status: string; search: string; onTechnician: (v: string) => void; onCampus: (v: string) => void; onStatus: (v: string) => void; onSearch: (v: string) => void }) { return <><label className="field calendar-search top-space"><span>Buscar</span><div className="input-with-icon"><Search size={16} /><input value={search} onChange={(event) => onSearch(event.target.value)} /></div></label><div className="calendar-filter-strip top-space"><label className="field"><span>Técnico</span><select value={technician} onChange={(event) => onTechnician(event.target.value)}>{technicians.length > 1 && <option value="">Todos los técnicos</option>}{technicians.map((item) => <option key={item.id} value={item.email}>{item.name}</option>)}</select></label><label className="field"><span>Sede</span><select value={campus} onChange={(event) => onCampus(event.target.value)}><option value="">Todas las sedes</option>{campuses.map((item) => <option key={item}>{item}</option>)}</select></label><label className="field"><span>Estado</span><select value={status} onChange={(event) => onStatus(event.target.value)}><option value="">Todos los estados</option>{statuses.map((item) => <option key={item} value={item}>{calendarStatusLabel(item)}</option>)}</select></label></div></>; }
