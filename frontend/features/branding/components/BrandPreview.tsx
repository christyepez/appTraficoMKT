import Image from "next/image";import type { BrandSettings } from "../models/branding.models";

export function BrandPreview({ settings }: { settings: BrandSettings }) { const header = settings.headerUseGradient ? `linear-gradient(${settings.headerGradientDirection}, ${settings.headerColor}, ${settings.headerGradientColor})` : settings.headerColor; const button = settings.useGradient ? `linear-gradient(${settings.gradientDirection}, ${settings.primary}, ${settings.gradientColor})` : settings.primary; return <section className="panel preview-section top-space" aria-label="Vista previa aislada"><h2>Vista previa</h2><article className="brand-preview top-space" style={{ borderColor: settings.accent, background: header, fontFamily: settings.fontFamily }}><Image unoptimized width={96} height={96} src={settings.logo} alt="Logo institucional"/><div>{settings.showHeaderTitle&&<h3 style={{color:settings.headerTitleColor,fontSize:settings.headerTitleSize,fontWeight:settings.headerTitleWeight,fontStyle:settings.headerTitleItalic?"italic":"normal",textDecoration:settings.headerTitleUnderline?"underline":"none"}}>{settings.title}</h3>}{settings.showHeaderSubtitle&&<p style={{color:settings.headerSubtitleColor,fontSize:settings.headerSubtitleSize,fontWeight:settings.headerSubtitleWeight}}>{settings.subtitle}</p>}</div></article><div className="actions top-space"><button type="button" style={{background:button,color:settings.buttonText}}>Principal</button><button type="button" style={{background:settings.secondary,color:settings.secondaryText}}>Secundario</button></div><small>La vista previa está aislada y no modifica el tema global hasta guardar.</small></section>; }
