import { Search } from "lucide-react";
import type { Technician } from "../models/agenda.models";
export function AgendaFilters({ technicians, selectedTechnician, search, onTechnician, onSearch }: { technicians: Technician[]; selectedTechnician: string; search: string; onTechnician: (value: string) => void; onSearch: (value: string) => void }) { return <><label className="field"><span>Técnico</span><select value={selectedTechnician} onChange={(event) => onTechnician(event.target.value)}>{technicians.map((item) => <option key={item.id} value={item.email}>{item.name} - {item.email}</option>)}</select></label><label className="field"><span>Buscar agenda</span><div className="input-with-icon"><Search size={16} /><input value={search} onChange={(event) => onSearch(event.target.value)} placeholder="Producto, técnico, título, notas..." /></div></label></>; }
