import type { UseFormSetValue, UseFormRegister } from "react-hook-form"; import type { UserFormValues } from "../schemas/user.schema"; import { defaultScreensForRole } from "../utils/user.utils";
export function RoleSelector({roles,register,setValue}:{roles:string[];register:UseFormRegister<UserFormValues>;setValue:UseFormSetValue<UserFormValues>}){return <label className="field"><span>Perfil</span><select {...register("role",{onChange:(event)=>setValue("screenPermissions",defaultScreensForRole(event.target.value),{shouldValidate:true})})}>{roles.map(role=><option key={role}>{role}</option>)}</select></label>;}
