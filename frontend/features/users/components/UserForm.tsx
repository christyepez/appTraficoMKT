"use client";
import { zodResolver } from "@hookform/resolvers/zod";import {useState}from"react";import{useForm,useWatch}from"react-hook-form";import{AccessibleDialog}from"../../../shared/components/AccessibleDialog";import{FormField as Field}from"../../../shared/components/FormField";import type{ManagedUser,SaveUserPayload}from"../models/user.models";import{userFormSchema,type UserFormValues}from"../schemas/user.schema";import{mapUserPayload,userDefaults}from"../utils/user.utils";import{RoleSelector}from"./RoleSelector";import{ScreenPermissionSelector}from"./ScreenPermissionSelector";
export function UserForm({user,roles,screens,onSave,onClose}:{user:ManagedUser|null;roles:string[];screens:string[];onSave:(user:ManagedUser|null,payload:SaveUserPayload)=>Promise<unknown>;onClose:()=>void}){const[submitError,setSubmitError]=useState("");const{register,handleSubmit,control,setValue,formState:{errors,isSubmitting}}=useForm<UserFormValues>({resolver:zodResolver(userFormSchema),defaultValues:userDefaults(user,roles)});const selected=useWatch({control,name:"screenPermissions"});async function submit(values:UserFormValues){setSubmitError("");try{await onSave(user,mapUserPayload(values));onClose();}catch(error){setSubmitError(error instanceof Error?error.message:"No se pudo guardar el usuario.");}}return <AccessibleDialog labelledBy="user-form-title" onClose={onClose} closeDisabled={isSubmitting}><div className="card-head"><h2 id="user-form-title">{user?"Editar usuario":"Crear usuario"}</h2><button autoFocus className="icon-button" type="button" aria-label="Cerrar formulario" disabled={isSubmitting} onClick={onClose}>×</button></div><form className="form top-space" noValidate onSubmit={handleSubmit(submit)}><input type="hidden" {...register("editing")}/><Field label="Nombre" error={errors.name?.message}><input {...register("name")}/></Field><Field label="Email" error={errors.email?.message}><input type="email" {...register("email")}/></Field><Field label={user?"Nueva clave temporal":"Clave inicial"} error={errors.password?.message}><input type="password" autoComplete="new-password" {...register("password")}/></Field><label className="field"><span>Proveedor</span><select disabled={Boolean(user)} {...register("authProvider")}><option>Local</option><option>Microsoft</option></select></label><label className="check-field field-wide"><input type="checkbox" {...register("allowMicrosoftLogin")}/> Permitir ingreso por Office 365</label><label className="field"><span>Menú del usuario</span><select {...register("menuMode")}><option value="horizontal">Horizontal</option><option value="vertical">Vertical</option></select></label><label className="check-field"><input type="checkbox" {...register("menuCollapsed")}/> Menú vertical plegado</label><RoleSelector roles={roles} register={register} setValue={setValue}/><ScreenPermissionSelector screens={screens} selected={selected} onChange={values=>setValue("screenPermissions",values,{shouldValidate:true})} error={errors.screenPermissions?.message}/><label className="check-field field-wide"><input type="checkbox" {...register("isActive")}/> Activo</label>{submitError&&<p role="alert">{submitError}</p>}<div className="form-actions"><button className="button" disabled={isSubmitting}>{isSubmitting?"Guardando":user?"Guardar":"Crear"}</button><button className="button secondary" type="button" disabled={isSubmitting} onClick={onClose}>Cancelar</button></div></form></AccessibleDialog>;}
