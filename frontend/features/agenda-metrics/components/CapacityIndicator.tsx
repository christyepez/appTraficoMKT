import type { AgendaMetricSummary } from "../models/metrics.models";
export function CapacityIndicator({ value }: { value: AgendaMetricSummary }) { return <section className="panel agenda-attention-panel"><h2>Atención del coordinador</h2><div className="attention-list top-space"><Item tone="danger" title={`${value.atRisk} productos en riesgo`} detail="Entrega próxima sin planificación visible." /><Item tone="warning" title={`${value.pendingPlanning} pendientes de agenda`} detail="Requieren bloque de trabajo técnico." /><Item tone="success" title={`${value.plannedHours.toFixed(1)} horas planificadas`} detail={`${value.occupancy}% de ocupación del periodo.`} /></div></section>; }
function Item({ tone, title, detail }: { tone: string; title: string; detail: string }) { return <div className={`attention-item ${tone}`}><span /><div><strong>{title}</strong><small>{detail}</small></div></div>; }
