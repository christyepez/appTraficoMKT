import{importScopes,type ImportScope}from"../models/import.models";export function ImportTypeSelector({value,onChange}:{value:ImportScope;onChange:(value:ImportScope)=>void}){return <label className="field"><span>Tipo de carga</span><select value={value} onChange={event=>onChange(event.target.value as ImportScope)}>{importScopes.map(item=><option key={item.value} value={item.value}>{item.label}</option>)}</select></label>;}
