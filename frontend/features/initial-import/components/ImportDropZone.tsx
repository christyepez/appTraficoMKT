export function ImportDropZone({file,onSelect}:{file:File|null;onSelect:(file:File|null)=>void}){return <div className="upload-drop field-wide" onDragOver={event=>event.preventDefault()} onDrop={event=>{event.preventDefault();onSelect(event.dataTransfer.files[0]??null);}}><strong>{file?.name??"Arrastre la plantilla o seleccione un archivo"}</strong><span>{file?`${(file.size/1024/1024).toFixed(2)} MB`:"Formato .xlsx, máximo 50 MB"}</span><label className="button secondary">Seleccionar archivo<input hidden type="file" accept=".xlsx" onChange={event=>onSelect(event.target.files?.[0]??null)}/></label></div>;}
