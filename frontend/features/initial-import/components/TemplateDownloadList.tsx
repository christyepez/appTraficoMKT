import{importScopes}from"../models/import.models";import styles from"../styles/InitialImport.module.css";import{templateHref}from"../utils/import.utils";export function TemplateDownloadList(){return <div className="catalog-tabs catalog-tabs-horizontal top-space">{importScopes.map(item=><a className={`tab ${styles.template}`} key={item.value} href={templateHref(item.value)} download>Plantilla {item.label}</a>)}</div>;}
